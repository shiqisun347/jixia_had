# 稷下人机自动辩论实验平台需求文档

> 文档状态：MVP 需求确认版  
> 更新日期：2026-08-03

## 1. 产品定义

### 062 席位交换申请补充

等待房间中，真人不可直接抢占其他真人席位。已选席位的辩手可以发送交换申请；对方确认后服务端事务性交换双方席位，设备检测和准备状态随用户保留。任一方离席、换席或比赛开始后，待处理申请自动失效。

“稷下人机交互平台”是一个可控、可复现的人机语音辩论实验平台。平台支持人类辩手与 Agent 辩手混合参赛，以可复用赛制规则自动推进比赛，通过实时 ASR、流式 LLM 与双流式 TTS 完成人机自然语音交互。

MVP 的核心目标：

- 同时组织最多 5 场处于“进行中”或暂停状态的比赛。
- 支持 1v1 至 5v5，以及全人类、全 Agent 和人机混合比赛。
- 保证发言权、计时、阶段推进、断线恢复和模型调用可控；优先采用可验证的有限规则，而不是通用工作流引擎。
- 优先保证实时 ASR/TTS 的质量、低延迟和长语音稳定性。
- 提供简洁、年轻、易操作的桌面端界面与完整管理后台。
- 保存可用于复盘与实验分析的文字、音频、评分和运行日志。

MVP 不包含摄像头、知识库、联网搜索、聊天室、评论、表情互动、短信服务、自动备份和专门的移动端适配。

## 2. 用户与权限

### 2.1 用户体系

- 所有人，包括观众，必须注册并登录。
- 注册字段：唯一用户名、真实姓名、密码、真人预设头像；用户可直接选择预设，也可选上传头像。
- 前台仅显示真实姓名，不展示用户名。
- 角色只有普通用户和管理员。
- 同一账号允许多设备登录，但只能保持一个活动房间连接；新连接进入房间时踢出旧房间连接。
- 用户参与一场未结束比赛后，不得以选手或观众身份进入其他房间；必须先结束或退出当前比赛状态。
- 用户名规范化后 3–32 字符且唯一，真实姓名 2–30 字符，密码 8–64 字符并使用 Argon2id；连续 5 次失败冻结 15 分钟。忘记密码由管理员生成临时密码。
- 头像只接受 JPEG/PNG/WebP、最大 2 MB，校验真实 MIME 后缩放为 256×256 WebP；不接受 SVG、GIF 或远程 URL。
- 首次以人类辩手身份参赛前，确认版本化的录音、ASR/LLM/TTS 处理、数据保留及登录用户可见范围；观众只需确认平台条款。MVP 不提供自助删除账号。

### 2.2 比赛权限

所有人类选手可以：

- 在自己的发言阶段提前结束发言。
- 重置自己当前尚未结束的发言。
- 暂停比赛（必须确认）。
- 编辑自己已经结束且取得 ASR 最终结果的发言。

房主额外可以：

- 开始比赛。
- 重置任意人类或 Agent 的当前发言。
- 申请恢复比赛。
- 终止比赛。

暂停发起者可以申请恢复比赛。Agent 和观众无暂停、恢复及比赛控制权限。

管理员可以进入实时控制页，查看和诊断比赛、暂停、恢复、终止、重置当前发言和重试失败服务。管理员不得强制跳过发言或阶段，不得更换选手、Agent、赛制或辩题，不得改写历史计时和发言权事件。重置不记录单独的重置事件，但系统诊断日志仍可记录必要的脱敏错误。

### 2.3 组织者与全 Agent 房间

- 普通房间的房主拥有组织和比赛控制权限；创建后默认以未选席辩手身份进入等待房间，再选择席位。房主可以在等待阶段切换为观众，也可以再切回辩手，控制权限不随该身份切换。
- 房间所有权由房主字段决定，成员身份只表示当前作为辩手或观众参与；纯 Agent 房的创建管理员使用不占席的组织者身份。
- Agent 只占辩手席，不作为房间成员；人类可以占用或替换 Agent 席位。
- 全 Agent 房间只能由管理员在后台创建和管理；普通用户只能观战，不能替换席位或接管控制。创建管理员离线不影响比赛，其他管理员可以管理。

## 3. 赛制规则

### 3.1 规则模型

赛制是可复用的规则模板，例如“新国辩”只是其中一种规则。每场比赛必须先选定赛制，比赛名称中的“正式赛”“训练赛”等仅作为数据标签，不改变比赛逻辑。

规则支持：

- 设置 1v1 至 5v5。
- 按顺序配置线性阶段。
- 复制规则、复制阶段、从模板创建。
- 修改规则后生成新版本；已经使用的比赛保留当时的规则快照。

MVP 的“任意规则”限定为有限原语的线性组合，不实现分支、循环、并行发言或动态改规则。可用阶段原语为：固定发言、自由辩论、固定准备时间、结束；暂停是运行时状态，不是规则动作。规则估算最长时长不得超过 90 分钟，启用前校验所有主持、准备和发言时长。仅管理员可创建、复制、编辑、启用或停用规则，普通房主只能选择已启用版本。规则状态为 `DRAFT → GENERATING_AUDIO → READY → ENABLED`；只为新增或变更的主持片段生成音频，全部成功并试听通过后才可启用。

### 3.2 线性阶段编排

结构为：`赛制规则 → 有序阶段 → 一个或多个发言动作`。

普通阶段可配置：

- 阶段名称和说明。
- 阶段开始、结束时播放的主持文本及音频。
- 发言动作顺序。
- 每个动作的阵营、辩位、允许发言时长和是否允许重置。

主持内容不播报动态辩手姓名。创建或修改赛制时预先生成主持文本和音频，比赛过程中只播放预生成音频，不临时调用 TTS。主持音频未全部生成成功的规则不可用于开赛。更换主持音色后，所有启用规则的主持音频必须重新生成。

## 4. 房间与比赛生命周期

### 4.1 公开大厅

- 所有房间均为公开房间，无私密开关。
- 大厅展示比赛名称、赛制、辩题、当前人数、状态和观战余量。
- 全平台同时最多 10 名观众；满员时显示“观战席已满”。
- 观众只接收当前人类发言、Agent 服务输出和主持/提示音，不发布音频；非当前人类音轨不播放给观众。
- 观众可查看席位、阶段、计时和完整实时文字记录，但不能控制或编辑。

### 4.2 创建与准备

房主创建房间时：

- 创建页默认选择已启用的 4v4 赛制；若平台没有可用 4v4 规则，再回退到其他已启用规则。用户仍可主动选择和自定义其他赛制。

1. 选择赛制规则。
2. 填写比赛名称或标签。
3. 从辩题库选择辩题，或自定义辩题。
4. 可为指定席位选择 Agent；未指定的席位由系统按稳定顺序自动补齐。创建完成时所有席位必须由互不重复的可用 Agent 预填。

普通房主不在创建页预选席位。创建成功后房主作为未选席辩手进入等待房间，再选择一个 Agent 席位并替换该 Agent。纯 Agent 房仅由管理员创建，创建者只组织和观战，不参与选席和设备检测。

用户进入房间后必须：

1. 选择“辩手席”或“观战席”；辩手选择一个未被人类占用的位置，人类可以占用并替换 Agent 席位。
2. 辩手完成麦克风授权与扬声器、麦克风、网络状态检测；观众只做连接检查，可选扬声器试听；仅组织者身份无需麦克风检测。
3. 辩手点击“准备”。

设备检测包括：麦克风权限、设备存在、输入音量、3 秒录音、扬声器短提示音、LiveKit 测试房连接、延迟和丢包。正常路径一次点击并在约 3–4 秒内并行完成；PASS 自动保存并准备，WARN 需用户确认，FAIL 必须处理原因后重试。录音试听和网络详情可选，不阻塞 PASS 准备。MVP 不限定浏览器，以实际检测结果为准。

等待房间使用“选择身份 → 选择席位 → 检测设备 → 准备完成”的明确步骤引导，步骤状态只依据服务端房间事实推导。观众加入后无需选席和检测；同时占席的房主属于真人辩手，同样可以检测并准备。真人和 Agent 使用不同头像形状、身份标签和配色，席位卡与操作区域在轮询、换席和补位时保持稳定，避免按钮跳动。

网络与录音使用同一个约 3 秒窗口并行采样并计算 P95：RTT ≤ 200 ms 且丢包 ≤ 3% 为正常；RTT 200–400 ms 或丢包 3%–8% 为警告，用户确认后可继续；RTT > 400 ms、丢包 > 8% 或断开时阻止准备。首次点击立即播放短提示音并完成浏览器音频解锁。有效检测属于房间内用户，不随席位变化失效；设备变更、权限丢失、音轨结束、有效期到期或 LiveKit 重新建立失败时才重新完整检测。

### 4.3 开赛条件

系统必须同时确认：

- 已选辩题。
- 所有规则要求的席位均由人类或 Agent 填满。
- 所有处于辩手身份的人类成员均已选择真人席位；不参赛者必须先切换为观众。
- 所有人类选手在线、仍在房间、设备可用且已准备。
- 所有 Agent 已启用并绑定可用音色。
- 规则所需主持音频已就绪。
- 当前“进行中”或暂停的比赛少于 5 场。

条件满足后执行 3 秒倒计时开赛。平台允许创建和准备更多房间，但第六场点击开始时不得开赛，显示“当前比赛容量已满，请稍后重试”。临时暂停和无限期暂停的比赛均占用名额，直到恢复、结束或由房主、管理员终止。

### 4.4 等待房间

- 普通等待房间房主离开或断开超过 60 秒时，转交给最早进入且在线的人类辩手；没有人类辩手时，将最早在线观众提升为组织者；无人时关闭。比赛开始后房主锁定，不再转交。全 Agent 管理员房间不适用该转交规则。
- 空房间立即关闭。
- 有用户但连续 2 小时无操作时，提前 5 分钟提示，随后关闭。
- 比赛开始后锁定房主，不再转移。
- 比赛开始后，选手主动离开按掉线处理，席位保留，且不能加入其他房间。
- 房主或管理员终止比赛后，释放全部选手及其房间占用状态。
- 一个房间只承载一场比赛；正常结束或终止后进入归档，不在原房间重赛。
- 普通用户最多占用一个未结束房间，普通房主最多拥有一个未关闭房间。管理员可在后台管理多个全 Agent 房间，但仍受全局 5 场活动比赛上限约束。
- 比赛开始前观众可在满足席位和设备条件时切换为辩手；开赛后身份与席位锁定。

## 5. 比赛编排与发言控制

### 5.1 服务端权威控制

比赛编排服务是阶段、计时、发言权和状态迁移的唯一权威来源。任一时刻只允许一名辩手发言，非当前发言者的麦克风由系统强制关闭，避免重叠和抢占。

主持音频结束后：

1. 页面显示悬浮字幕动画“轮到你发言了！”。
2. 人类看到“开始发言”按钮，点击后才开启麦克风、ASR 和正式计时；不执行个人 3 秒倒计时。
3. Agent 在主持音频期间提前准备，主持音频结束后直接播放；未就绪时显示“Agent 正在思考中”。

人类发言中不提供手动静音/重新开启；仅提供“提前结束发言”和有权限者可见的“重置本次发言”。系统在到时或提前结束后停止采集并等待 ASR final。单次业务发言不得超过 3 分钟，底层 ASR task 会透明轮换。

计时起点：

- 人类：用户点击“开始发言”且服务端确认采集开始时。
- Agent：首个 TTS 音频包实际开始播放时。

主持音频、LLM 等待、TTS 首包等待、ASR 最终结果等待，以及自由辩论的举手窗口均不占用发言时长。

人类“开始发言”按钮出现后有 60 秒等待窗口；超时未点击则显示具体原因并暂停全局比赛，不由 Agent 接管。全局恢复后的等待窗口重新从 60 秒开始。人类在发言中暂停时，当前 ASR 段先结束、文字和音频保留，恢复后经全局 3 秒倒计时仍需手动开始下一段，剩余时长继续累计。

发言结束后 final 最长等待 3 秒。无有效人声时询问是否放弃本次发言；确认后才提交空发言。已有清晰人声但 final 为空视为 ASR 失败，按重试规则处理。

### 5.2 重置当前发言

重置只作用于当前尚未结束的发言，必须弹出确认框：

- 立即停止当前 ASR、LLM 或 TTS。
- 丢弃本次未完成的音频和文字，不写入辩论上下文。
- 恢复完整发言时长；全局恢复时统一播放预设提示并倒计时 3 秒，之后人类仍需手动点击“开始发言”。
- Agent 发言被重置时重新调用 LLM 和 TTS。
- 不保留单独的重置事件；仅保留当前有效发言和必要的脱敏诊断信息。

## 6. 自由辩论

### 6.1 时间与轮次

- 双方严格交替获得发言权，不允许同一方连续主动发言。
- 双方分别拥有自由辩论总时长，并配置单次最大发言时长。
- 只有当前发言方消耗本方剩余时间。
- 一方时间耗尽后，另一方可连续使用自己的剩余时间。

### 6.2 人类举手

- 从对方发言开始，到对方发言结束后 3 秒内，当前候选方的人类辩手可以申请发言。
- 点击申请后，头像旁显示举手图标和申请顺序；再次点击可取消。
- 人类申请与本方 Agent 的主动申请组成同一逻辑队列；人类始终排在 Agent 前，多个人类按服务端受理时间排序，多个 Agent 按意愿值降序排序，意愿相同时按固定席位顺序排序。
- 队列在窗口内动态更新；人类取消后移出队列，再次申请按新的受理时间排在人类队列末尾。窗口结束后锁定队列，第 1 名获得发言权。
- 举手、Agent 决策状态和队列名次只对本方人类辩手及管理员可见；对方和观众不能从 HTTP 或 WebSocket 响应中取得这些队内字段。最终发言者身份对所有人公开。
- 候选方没有在线人类辩手时跳过无意义的 3 秒举手窗口。
- 人类获得发言权后仍需手动点击“开始发言”，并适用 60 秒开始窗口。

### 6.3 Agent 候选决策

Agent 的“是否发言”决策只存在于自由辩论阶段。

对方发言最终文字确定后，无论此时是否已有真人举手，系统都为本方每个可用 Agent 并行发起独立的快速决策调用：

```json
{
  "should_speak": true,
  "willingness": 0.86
}
```

每个 Agent 在调用开始时显示“决策中”；单个结果返回后立即显示“举手”或“跳过”，不等待其他 Agent。自动重试期间仍显示“决策中”，重试后失败在普通页面显示“跳过”并保存真实失败信息。所有决策完成或超时且人类申请窗口关闭后，系统锁定混合队列并进入选择：

- 若有人类举手，选择人类队列第一名；Agent 决策仍完成并保存，但不生成 Agent 正式发言。
- 若无人类举手且存在 `should_speak=true` 的 Agent，选择 Agent 队列第一名。
- 若无人举手但存在有效决策，在全部 `should_speak=false` 的 Agent 中按意愿值降序、席位升序选择补位者。
- 若所有 Agent 决策重试后均失败且无人类举手，使用 `match_seed` 在本方可用 Agent 中确定性选择补位者；自由辩论快速决策失败是核心失败暂停规则的特例，不因此暂停比赛。
- 连接、首 Token 或流式停顿超过 3 秒视为失败并自动重试一次；部分失败不阻塞其他结果，失败记录不参与意愿排序。
- 决策期间出现或取消人类申请不会取消 Agent 调用；队列按最新状态动态重排。
- 只有最终选中的 Agent 会生成正式发言文本；不得为未选中 Agent 创建 TTS。
- Agent TTS 不得在举手窗口结束前播放。
- 举手窗口结束后，只为最终选中的 Agent 创建一个 TTS 任务。
- 每次 Agent 决策长期保存 `should_speak`、`willingness`、耗时、尝试/失败、最终名次、是否实际发言及当时真人申请状态；不作为正式辩论内容或上下文。
- Agent 生成全文只在实际播放后按播放进度成为正式文字；未播放文本不进入公开记录或上下文。

Agent 的 LLM 输出不经过 ASR。LLM 文本块持续发送给双流式 TTS，由服务端处理缓冲和合成，不等待完整句子；界面只按实际播放进度展示对应文字。

## 7. 实时语音链路

### 7.1 ASR

MVP 使用阿里云百炼 `fun-asr-realtime`：

- WebSocket 双流式输入输出。
- 输入为 16 kHz、单声道 PCM。
- 用户侧支持最长 180 秒的连续发言；服务端在同一个 `speech_id` 内约每 25 秒透明轮换 ASR task，单 task 不超过 30 秒，切换期间最多缓存 1 秒 PCM，并按顺序拼接各段最终文本。
- 使用精简的全局热词表。
- 动态上下文增强默认关闭，以减少延迟；后台保留启用能力。
- 前端实时展示中间结果，但中间文字只读。
- 数据库仅保存最终结果、首次中间结果延迟、最终结果延迟、音频时长和错误信息，不保存全部中间修订过程。
- 首次建连、task 或流式处理失败时，系统清理当前未完成发言、退还完整时长并提示人类再次手动点击“开始发言”；第二次仍失败才暂停比赛。系统不自动开麦。

### 7.2 TTS

MVP 使用阿里云百炼 `qwen-audio-3.0-tts-flash`：

- 使用官方 WebSocket `run-task → continue-task → finish-task` 协议。
- LLM 文本流与 TTS 音频流同时进行，形成真正的双流式链路。
- 文本由服务端持续追加，依靠模型服务完成缓冲、分段和合成时机控制。
- 百炼返回 32 kbps Ogg Opus，服务端一边写入临时压缩 spool，一边由 PyAV/libav 增量解码为 48 kHz 单声道 PCM，再通过 LiveKit `AudioSource` 发布。
- FFmpeg 只用于赛后标准化、合并和诊断，不进入实时播放链路；保留显式配置的 PCM 回退方案。
- 语音只要求清楚、自然、音色稳定；优先控制延迟、带宽和数据量。实时链路总播放缓冲控制在 200–300 ms，FFmpeg 只用于赛后处理。

`server_commit` 属于 Qwen-TTS Realtime 的提交模式，不是本项目所选 Qwen-Audio-TTS 接口参数，MVP 不使用该名称配置协议。

Agent 暂停、重置或播放故障时不做精确续播：取消当前 TTS、丢弃未完整播放的文字和音频，恢复完整允许时长，并从头重新生成/合成。TTS 首次失败可保留已完成的 LLM 草稿并从头重合成；第二次失败不提交正式文字、音频或上下文并暂停比赛。Agent 正式文字只包含完整实际播放的内容；超时时缺少可靠字级时间戳时仅保留已完整播放的句子，并标记 `audio_truncated=true`。

### 7.3 音频传输

- 浏览器与 LiveKit 之间使用单声道 Opus，目标码率约 24 kbps。
- 人类音轨在服务端解码并重采样为 16 kHz PCM 后送入 ASR。
- 默认开启回声消除、噪声抑制和自动增益；管理员可为实验关闭。
- 不启用视频。
- 观众只订阅当前人类发言音频和服务端输出轨，降低服务器出口带宽；不订阅其他人类原始音轨。

## 8. 音色方案

后台只维护一套全平台通用语音方案。Agent 配置绑定具体 TTS 音色；主持音色独立固定。系统提供试听、启用/停用、更换和语速重新校准功能。每个 Agent 音色必须保存一个与其年龄感、性别表达和气质相符的 Agent 头像；该映射是 Agent 头像的唯一来源。创建 Agent 或切换音色时头像自动同步，Agent 管理不允许独立修改头像。

### 8.1 Agent 预置音色

| # | 音色 | 性别/年龄 | 风格 | 官方 voice ID |
|---|---|---|---|---|
| 1 | 龙莹松柳 | 男 / 32 | 爽朗利落 | `qwen-audio-3.0-tts-flash-longyingsongliu` |
| 2 | 龙昕蕊璇 | 男 / 24 | 自然亲和 | `qwen-audio-3.0-tts-flash-longxinruixuan` |
| 3 | 龙涟霓蓉 | 男 / 42 | 平稳陈述 | `qwen-audio-3.0-tts-flash-longliannirong` |
| 4 | 龙蓉桃涟 | 男 / 34 | 冷静沉稳 | `qwen-audio-3.0-tts-flash-longrongtaolian` |
| 5 | 龙翼暮凌 | 男 / 38 | 沉稳有力 | `qwen-audio-3.0-tts-flash-longyimuling` |
| 6 | 龙璨竹月 | 女 / 26 | 平实质朴 | `qwen-audio-3.0-tts-flash-longcanzhuyue` |
| 7 | 龙风月瑶 | 女 / 28 | 直爽利落 | `qwen-audio-3.0-tts-flash-longfengyueyao` |
| 8 | 龙弦星嵐 | 女 / 28 | 自然交流 | `qwen-audio-3.0-tts-flash-longxianxinglan` |
| 9 | 龙漪暄珺 | 女 / 36 | 成熟沉稳 | `qwen-audio-3.0-tts-flash-longyixuanjun` |
| 10 | 龙晴霄湘 | 女 / 27 | 自信从容 | `qwen-audio-3.0-tts-flash-longqingxiaoxiang` |

### 8.2 主持音色

| 音色 | 性别/年龄 | 风格 | 官方 voice ID |
|---|---|---|---|
| 龙露柳澈 | 男 / 34 | 标准播音 | `qwen-audio-3.0-tts-flash-longluliuche` |

### 8.3 音色语速校准

预置 11 个 profile（10 个 Agent、1 个主持）仅代表候选配置；每个 profile 必须通过真实双重 Opus 播放链路、时长/时间戳/延迟测试和管理员试听后才可启用。至少 10 名中文听众使用 3 段辩论文本和实际低/高语速盲听，清晰度平均 ≥ 4.0/5、自然度 ≥ 3.5/5、一致性 ≥ 4.0/5；至少 4 个 Agent 音色和 1 个主持音色通过后才允许正式实验。

音色首次启用时，使用固定中文测试文本生成音频，以实际音频时长计算每秒中文字数。Agent 单次输出长度按以下方式估算：

`目标字数 = 阶段允许时长 × 实测每秒字数 × 0.85`

服务端再将目标字数换算为 `max_tokens`。不要求管理员手工填写语速，也不向 LLM 传递双方剩余时间或允许发言时长。管理员可以重新校准。TTS 音频超过阶段时间时，到时直接停止播放。

## 9. Agent 与模型管理

### 9.1 模型设置

支持导入多个 OpenAI 兼容模型端点，字段包括：

- 模型名称和模型 ID。
- Base URL。
- API Key。
- 是否启用，默认使用流式输出。
- 单模型最大并发数；全局默认硬上限为 50，单模型配置不得超过该值。普通发言、自由辩论决策/生成、AI 裁判和后台测试共享该上限，按优先级排队。

连接测试必须覆盖普通响应、流式响应、首 Token 延迟、结构化 JSON 输出。管理员可以直接查看和复制完整 API Key，但 API Key 不得出现在系统日志或普通数据导出中。

### 9.2 Agent 配置

每个 Agent 配置：

- 名称、启用状态。
- LLM 模型。
- 系统提示词、辩手提示词。
- 温度等生成参数。
- 绑定的 TTS 音色。

管理员可以编辑未被进行中、启动中或暂停比赛引用的 Agent 配置；活动比赛引用期间，影响运行行为的字段锁定并显示具体原因。编辑成功后新房间使用最新配置，已有比赛继续使用启动时快照。名称重复、模型/音色停用和编辑锁定均显示可理解的操作提示。

默认向 Agent 提供完整的最新辩论上下文，不做摘要和截断。模型按 1M 上下文设计，但每次调用前仍检查输入长度并预留输出空间。

建议请求上下文：

```json
{
  "model_name": "模型名称",
  "debater_name": "Agent 名称",
  "debate_side": "正方",
  "debate_position": "二辩",
  "debate_topic": "辩题",
  "current_stage": "自由辩论",
  "next_stage": "反方四辩总结",
  "holder": "正方",
  "debate_history": [
    {
      "stage": "正方一辩立论",
      "message": [{"speaker": "正方一辩", "content": "……"}]
    }
  ]
}
```

用户、Agent、席位和比赛均使用稳定 ID，显示名称不能作为关联键。普通指定发言只输出纯文本流；自由辩论决策输出固定 JSON；AI 裁判输出固定评分 JSON。

### 9.3 调用超时和并发

- 建立连接或首个 Token 超过 10 秒：调用失败。
- 流式输出中连续 10 秒无新 Token：调用中断。
- 所有调用失败后自动重试一次；仍失败则显示具体原因并暂停比赛。
- 单模型超过并发上限时，按比赛轮转公平排队，最多等待 3 秒。
- 3 秒仍无容量时显示“模型并发已满”并暂停比赛。
- 不自动切换其他模型、Agent 或跳过发言。

模型、Agent、音色和裁判的运行字段在被活动或暂停比赛引用期间锁定；没有活动引用时管理员可以原地编辑，历史比赛使用启动时快照。模型连接测试、音色试听或校准结果在相关字段修改后失效并需要重新执行。赛制版本不可原地修改，使用复制新版本。API Key 可原地轮换，记录凭据版本和时间，已建立的调用继续使用旧连接。

## 10. 暂停、掉线与故障恢复

### 10.1 人类掉线

- 掉线后头像旁绿色圆点变灰，并显示“离线”。
- 掉线后的前 1 分钟比赛继续；如果正好轮到该用户发言，则暂停该发言倒计时。
- 连续离线满 1 分钟后，自动全局暂停比赛。
- Agent 不得接管人类席位。
- 用户重连后仍回到原席位。

### 10.2 手动暂停与恢复

- 人类选手暂停时无需填写原因，但必须确认。
- 暂停期间冻结阶段计时、发言计时、ASR、Agent 调用和 TTS。
- 暂停发起者或房主可以申请恢复。
- 恢复前检查所有人类选手在线、设备可用且仍在房间。
- 条件不满足时展示逐项具体原因。
- 条件满足时播放预设提示音，执行 3 秒倒计时后恢复。
- 暂停期间冻结计时、ASR、Agent 调用和 TTS；不产生新的外部调用。

### 10.3 服务故障

ASR、TTS、LLM、RTC 等核心链路失败时均自动重试一次；再次失败后先用可自动消失的悬浮 Toast 通知用户，再在比赛页右侧状态抽屉展示错误原因并暂停比赛。该抽屉同时显示已有辩论过程，便于结合上下文排查。普通房间操作、设备检测、准备、席位冲突、房间号解析和邀请复制失败均只使用统一 Toast，不在页面重复显示红色错误块。自由辩论快速决策是明确例外：单个最终失败在队内显示为“跳过”并继续，全部失败且无人举手时使用 `match_seed` 确定性兜底；正式 Agent 文本生成和 TTS 仍适用核心暂停规则。录音失败是非核心故障：重试一次后继续比赛并用 Toast 显示“录音不完整”提示。

AI 裁判是例外：评分失败不改变比赛已经结束的状态。系统重试一次后仍失败则显示评分失败，房主或管理员可以重新评分。

### 10.4 服务重启恢复

- 阶段切换、发言开始和发言结束时持久化比赛状态。
- 服务异常重启后，所有未结束比赛进入“系统恢复暂停”。
- 已完成发言和上下文保留。
- 中断时正在进行的 ASR 或 Agent 发言丢弃。
- 人类重新进入并完成设备检查后，由房主申请恢复。
- 当前人类或 Agent 发言从起点重新开始；Agent 不做精确音频 seek，未完成片段不进入正式上下文。
- 无法恢复时，房主或管理员可以终止比赛。

## 11. 文字记录与上下文

比赛页面右侧提供“文字记录”面板：宽屏常驻、较窄桌面使用抽屉，按阶段展示包括其他辩手在内的完整实时辩论过程。实时识别和 Agent 播放文字只在该面板显示，中央舞台不重复显示字幕；记录区域独立滚动，不随内容扩张整页。

- 正在识别的中间文字只读，避免人工编辑被 ASR 覆盖。
- 用户只能编辑自己已经结束并获得 ASR 最终结果的发言。
- 保存后用户界面只保留修改后的最新文字，不展示历史版本；数据库另存不可变 `asr_raw_final_text` 供实验评估，不能作为后续上下文。
- 全局唯一上下文立即切换到最新版本，之后的 Agent 决策和发言使用修改后的内容。
- 已生成或已经播放的 Agent 发言不回滚、不重新生成。
- 每次 LLM 调用保存脱敏的输入快照和调用开始时锁定的 `context_version`，用于实验复核；不保存密钥、用户 ID 或未脱敏个人信息。

比赛结束后：

- 所有参赛用户可以查看完整记录并审阅、修改、提交自己的发言。
- 所有人提交后归档；未提交内容在 24 小时后按最新文字自动提交。
- 正常结束的比赛对已登录用户公开完整文字和评分；终止比赛仅参与者、房主和管理员可见，不评分、不计排行榜、不进入公开历史。
- 音频只对参赛者和管理员开放。

## 12. AI 裁判与排行榜

### 12.1 裁判配置

Agent 管理下设置独立“AI 裁判”页面，配置 OpenAI 兼容模型、提示词、生成参数、启用状态和连接测试。

比赛正常结束后立即评分，始终以结束时文字版本为准。用户之后修改文字不会自动触发重新评分；房主或管理员手动重新评分时，使用点击时的最新文字版本。评分失败不改变已结束状态，重试一次后标记失败。

### 12.2 评分结构

- 正反双方各 100 分：立论 30、反驳 25、事实与证据 20、团队协作 15、表达与规则 10。
- 每名辩手个人评分 0–20 分。
- 输出获胜方：正方、反方或平局。
- 输出双方简评和每名辩手一句评语。

### 12.3 排行榜

主页分别展示人类辩手榜和 Agent 榜。MVP 只提供总榜，不提供周榜、月榜或赛季。

- 胜 30 分，平 20 分，负 10 分。
- 加个人评分 0–20 分，单场最高 50 分。
- 展示累计积分、比赛数、胜场、胜率和平均个人评分。排行榜由每日任务完整重算并生成快照，页面显示“更新于”；任务失败时保留上次快照并重试一次。
- 正常结束且 AI 评分有效的全人类、人机混合或全 Agent 比赛，分别计入对应的人类榜和 Agent 榜；终止比赛不计入。
- 提前终止的比赛不计分。
- Agent 被停用后保留历史数据，但不能参加新比赛。

## 13. 数据、录音与下载

### 13.1 实时记录

- 每场比赛由独立音频任务订阅 LiveKit 音轨。
- 人类 PCM 在送入 ASR 的同时异步写入分段录音。
- Agent TTS 音频在发布的同时保存。
- 实时过程中不做混音和压缩，录音失败不得阻塞比赛主链路。

### 13.2 赛后处理

- 异步合并整场音频并生成 Opus 回放文件和下载包。
- 处理期间显示“音频处理中”。
- 参赛者可下载完整文字、AI 评分、整场合并音频和自己的原始音轨。
- 管理员可下载全部音轨、日志和模型调用数据。
- 观众不能下载。

管理员可对单场、多场或当前筛选结果创建研究数据导出。列表快速导出使用带 UTF-8 BOM 的 CSV；完整研究包由后台异步生成 ZIP，至少包含 `manifest.json`、`transcript.md`、`speeches.csv`、`agent-calls.jsonl`、`events.jsonl`、`judge.json` 和 `files.csv`。音频默认不打包，管理员可显式选择包含。导出默认包含用户名、真实姓名和参赛身份；密码、Session、API Key、请求头、内部路径和原始堆栈永不进入导出。

尚未结束的比赛可导出。导出只包含任务开始时已提交的数据，并在清单中记录比赛状态、截止时间、`room.sequence`、`context_version` 和 `incomplete=true`；导出不暂停比赛，之后产生的数据需重新导出。导出包使用版本化 Schema，清单为每个文件保存字节数、记录数和 SHA-256 校验值。

### 13.3 保存周期

- 文字和比赛元数据：长期保存。
- 原始独立音轨：30 天。
- 合并 Opus 回放：90 天。
- 管理员可将指定比赛设置为永久保留或主动删除。
- 每日执行过期文件清理。
- 后台显示磁盘占用和按当前增长速度估算的可用天数。
- 导出文件保留 7 天，导出任务记录保留 90 天，过期后可重新生成。
- 模型调用、举手决策、比赛事件和其他实验业务数据随比赛长期保留，直到管理员删除整场比赛。
- 磁盘使用率超过 80% 告警；达到 90% 时禁止新比赛开始、新导出和可选音频打包，但不中断正在进行的比赛。
- MVP 不做自动备份。

## 14. 管理后台

后台采用成熟项目的交互模式改造，避免从头构建通用管理能力。参考 `satnaing/shadcn-admin` 的表格/行菜单/编辑抽屉、`arhamkhnz/next-shadcn-admin-dashboard` 的 Next.js 16 后台 Shell，以及 `marmelab/shadcn-admin-kit` 的 CRUD 资源语义；不直接复制模板代码，不引入完整后台框架。管理端与用户端视觉分离，管理端以专业、克制、信息密度和可读性优先。

后台模块：

1. 数据概览。
2. 用户管理。
3. 模型设置。
4. Agent 管理。
5. AI 裁判。
6. 赛制规则。
7. 辩题管理。
8. 语音方案。
9. 比赛与数据管理。
10. 日志管理。
11. 系统设置。

### 14.1 管理后台交互规范

- 用户、比赛、Agent、模型、音色、辩题和日志使用统一数据表格；创建/编辑使用右侧抽屉；赛制编辑和比赛实时控制保留独立全页面。
- 表格提供搜索、状态筛选、排序、结果数量和明确空态；危险操作通过行菜单和确认框完成。
- 启用/停用使用高对比度状态标签和操作菜单，不使用与背景接近的浅色无语义按钮。所有按钮明确声明背景、文字、边框、hover、focus 和 disabled 状态。
- 普通操作错误使用全局 Toast 并可重试；表单失败保留抽屉和输入；需要持续处理的比赛故障保留状态抽屉。
- 后台首页展示活动比赛容量、启用目录数量、后台任务/故障、磁盘门槛和最近比赛，指标可以进入对应列表；MVP 不做复杂监控图表。
- 桌面端主要支持 1440–1920 px，1024 px 以上完整操作；小于 1024 px 只保证基本查看和简单操作。
- 第一阶段先实施后台设计系统、运行总览和 Agent 管理试点；完成截图评审后再迁移其余页面。

#### 14.1.1 目录编辑边界

- Agent、模型、音色和辩题提供编辑能力，并在活动比赛引用时锁定影响运行或历史快照的字段。
- API Key 支持原地轮换并审计；其他模型端点字段在活动引用时锁定。
- 音色供应商标识、语速或校准变化后必须使试听状态失效并重新生成；主持音色变化按既有规则要求重新生成主持资产。
- 赛制不原地改写已启用版本，使用复制新版本后编辑。

### 14.2 用户管理

- 用户增删改查、启用和停用。
- 查看其比赛参与数、胜负、积分等统计。
- 没有比赛历史的用户可物理删除。
- 有历史数据的用户只能停用：保留真实姓名和历史关系，头像恢复默认，从排行榜移除，并允许以后恢复。MVP 不提供用户自助删除。

### 14.3 比赛与数据管理

每场比赛作为一个独立条目：

- 进行中或暂停比赛可进入实时控制页。
- 已结束比赛可查看和修改比赛标签、显示辩题、最终文字、AI 分数、获胜方和备注。
- 终止比赛保留已完成数据供参与者、房主和管理员查看，但不评分、不计排行榜、不进入公开历史。
- 修改分数或获胜方后重新计算排行榜。
- 不允许修改原始音频、事件日志、模型调用日志和实际开始、结束时间。
- 不允许后台伪造新比赛记录。
- 删除比赛时删除其文字、评分、排行榜积分、音频和业务数据，但永久保留管理员删除操作日志。

比赛详情使用独立工作台，按需加载概览/控制、参赛者/席位、文字记录、事件/举手、Agent 决策/调用、ASR/TTS、裁判、文件/导出。每次 Agent 调用长期保存实际发送的完整 `messages`、模型和参数、调用类型、重试、上下文版本、完整最终输出、Token、首 Token 延迟、总延迟、状态和脱敏错误。不保存逐 Token 块、原始 SSE、隐藏思维链、供应商原始响应、请求头或密钥。

自由辩论举手和决策数据按结构化事件保存：决策轮次、Agent 决策开始/结果/重试、人类举手/取消、每次权威重排、最终选中者和选择规则。不保存前端轮询或渲染快照。管理员可以标记异常、添加内部注释或排除分析，但不得覆盖原始 Agent 请求/回复、ASR raw、举手/决策事件、延迟或错误。

后台支持导出选中项、当前页或全部筛选结果。运行中比赛允许导出不完整时点快照；仅终态比赛可永久删除。删除前显示关联记录数和文件占用，删除后清理音频与导出包，审计日志仅保留稳定 ID 和脱敏操作摘要。

### 14.4 日志管理

日志与诊断分为：

- 不可变的管理操作与安全审计。
- 比赛事件与举手/决策事件。
- LLM、ASR、TTS 和裁判外部调用。
- 持久化的结构化 WARNING/ERROR 与事故聚合。
- 后台任务执行记录。
- journald 中的原始 INFO/DEBUG 运行日志。

支持按时间、服务、用户、比赛、调用类型、状态、级别、错误码和关联 ID 筛选，查看摘要后按需加载大文本详情。审计、比赛原始事件和调用原始记录只读；事故可确认、添加内部注释和标记解决，但不覆盖其原始发生记录。

管理审计永久保留；比赛事件和实验调用随比赛保留；PostgreSQL 中的系统 WARNING/ERROR 保留 180 天；后台任务执行记录保留 90 天；journald 原始日志保留 30 天。journald 下载必须选择服务和时间范围，再次脱敏后流式生成。密码、Session、Cookie、Authorization、API Key、数据库连接串和供应商原始响应不得写入数据库日志或导出。

系统按服务、错误码、异常类型和脱敏代码位置形成事故指纹，展示首次/最近发生、次数、受影响比赛/用户和关联 ID。事故状态为待处理、已确认、已解决；已解决事故再发时自动重开。MVP 只提供管理员站内事故提醒，不接短信、邮件、Webhook 或新的外部可观测服务。

### 14.5 管理列表查询

- 用户、比赛和日志使用服务端分页、搜索、筛选和排序，响应返回结果总数与当前页信息；不得静默只返回固定前 100/200 条。
- 比赛列表一次批量取得房间展示元数据、文件统计和状态，禁止逐条查询造成 N+1。
- Agent、模型、音色、辩题和规则目录规模较小，可先使用目录快照与客户端筛选；后续规模增长时再迁移服务端分页。

### 14.6 批量管理

- 后台支持具体选中项、当前页和全部筛选结果三种范围；筛选条件改变时清空已选项。
- 用户支持批量启用/停用/导出，不批量物理删除；Agent、模型、音色和辩题支持批量启用/停用/导出；赛制支持批量停用/导出，修改继续使用新版本；比赛支持批量导出、保留设置和终态永久删除。
- 被 `START_PENDING_RUNTIME`、`START_COUNTDOWN`、`RUNNING`、`PAUSED`、`SYSTEM_RECOVERY` 或 `ERROR` 比赛引用的用户、Agent、模型或音色不得停用。终态历史引用不阻止停用，但仍阻止物理删除。
- 所有批量变更先由服务端预检，显示可操作数、拒绝数和原因，危险操作再次确认。每个目标独立事务，允许部分成功并返回逐项结果。
- 批量任务使用幂等键、有界 PostgreSQL 任务队列和逐项状态；进度页在可见且任务未结束时每 2–3 秒轮询，不新增 WebSocket、Redis 或消息队列。
- 表格的筛选、排序和分页写入 URL；列显示、密度等偏好仅保存在当前浏览器，不增加服务端视图管理。

## 15. 技术架构与部署

### 15.1 MVP 架构

- Caddy 或 Nginx：TLS 和反向代理。
- Next.js：用户端和管理端 Web UI。
- FastAPI：业务 API、比赛编排、权限、ASR/TTS/LLM 适配和异步任务。
- 自托管 LiveKit Server：实时音频传输。
- LiveKit 前端 SDK 与 Python RTC SDK：客户端接入及服务端音频订阅、发布。
- PostgreSQL：业务数据和比赛状态。
- 本地文件系统：分段音频、合并音频和导出包。
- systemd：进程托管和开机自启。

MVP 使用模块化单机架构，不使用 Redis、Kafka、阿里云 OSS、Kubernetes 和 LiveKit Egress。只有确有必要的独立服务才使用容器，不以 Docker Compose 作为默认部署方式。

LiveKit 只负责 RTC 传输。比赛逻辑由自研编排服务控制；ASR、TTS、LLM 使用薄适配层；录音由自研音频任务完成，不引入完整 LiveKit Agents 框架。

### 15.2 建议服务器

- 中国大陆、尽量靠近阿里云百炼北京区域。
- 4 核 CPU、8 GB 内存、100 GB SSD。
- 20 Mbps 对称带宽。
- 部署后必须通过 5 场并发压测确认实际容量。

## 16. UI 与交互约束

MVP 以桌面端为主，风格年轻、简洁、现代，操作流程尽量傻瓜化，但不删减必要状态和控制能力。

现有视觉参考：

- `debate_page.png`：比赛页的主要高保真参考，布局和整体表现可近似复刻。
- `index.png`：主页视觉风格参考，不要求原样复制。
- `logo.png`：平台 Logo 参考。

比赛页重点结构：红蓝双方席位、中央当前发言者与声波、阶段和计时、主要控制按钮、右侧文字记录、连接和设备状态。1280×720 及以上桌面视口优先在一屏内展示全部主要内容，文字记录独立滚动。动画用于表达真实变化的开赛/恢复倒计时、当前发言权、举手顺序、麦克风和语音状态，保持流畅克制，不影响操作和性能。

建议技术：Tailwind CSS、shadcn/ui、Framer Motion、Lucide Icons；特殊图标使用自定义 SVG。后续实现使用 `ky-design-to-html` 流程，以固定参考画布开发，通过浏览器截图逐轮对比修正，确保高保真还原。

UI 原型切片只实现主页和比赛页，不接真实后端、认证或 LiveKit。使用 Storybook + `@storybook/nextjs-vite` 维护 Waiting、HumanReadyToStart、HumanSpeaking、AgentThinking、AgentSpeaking、FreeDebateHandRaise、Paused、Disconnected、ErrorDrawer、Finished 等状态；MSW 返回契约形状数据，Playwright 做 1440×900 主画布及 1280×720、1920×1080 冒烟。不另建 `/dev/scenarios`，MVP 不接 Chromatic。

## 17. 性能与验收标准

延迟均以服务端时间戳统计，并同时记录 P50、P95 和失败率。

| 指标 | MVP 验收目标 |
|---|---:|
| ASR 首次中间结果 | P95 ≤ 500 ms |
| 音频结束至 ASR 最终结果 | P95 ≤ 1000 ms |
| 首个 LLM 文本块至首个 TTS 音频包 | P95 ≤ 800 ms |
| Agent 响应间隔（强制主持/举手窗口结束至首个 PCM 入队） | P50 ≤ 800 ms；P95 ≤ 1500 ms |
| 房间关键状态同步 | P95 ≤ 300 ms |
| 连续 ASR 时长 | 用户侧连续 180 秒；底层透明轮换 task，无音频丢失 |
| 同时活动比赛 | 5 场，无明显性能下降、串音或状态泄漏 |
| 全平台观众 | 最多 10 人 |

必须进行以下验收：

- 5 场比赛并发，包含人类 ASR、Agent LLM/TTS、观战和录音。
- 180 秒连续语音识别，检查中间结果、最终结果、断流和内存增长。
- 真人举手与 Agent 决策并行时的动态混合队列、同方可见性和迟到结果测试。
- 自由辩论两阶段候选：人类始终排在 Agent 前、Agent 按意愿排序、全 false 取最高意愿、全部失败的 `match_seed` 兜底。
- 掉线 60 秒、重连、暂停和恢复测试。
- ASR、TTS、LLM、RTC 各自失败及重试后暂停测试。
- 服务重启后的比赛恢复测试。
- 文本修改后全局上下文即时更新测试。
- 磁盘超阈值和数据过期清理测试。

## 18. MVP 完成标准

满足以下条件视为 MVP 可投入实验使用：

1. 可配置并版本化 1v1 至 5v5 线性赛制。
2. 可从公开大厅完成注册、入房、选位、设备检测、准备、比赛和观战全流程。
3. 人类与 Agent 可在同一场比赛中按规则稳定交替发言。
4. 自由辩论的人类优先混合队列、Agent 渐进决策、全 false 最高意愿和全部失败兜底逻辑正确。
5. Fun-ASR 与 Qwen-Audio-TTS 实现真实双流式链路并达到延迟指标，11 个音色中至少 4 个 Agent 音色和 1 个主持音色通过实际播放链路盲听。
6. 5 场比赛并发运行，暂停、掉线、故障和重启均可控恢复。
7. 文字修改、全局上下文、录音、评分、排行和权限逻辑一致。
8. 管理员可管理用户、模型、Agent、裁判、赛制、辩题、音色、比赛和日志；可追溯 Agent/语音/举手数据，完成受控批量操作、导出和事故处理。
9. 界面以现有参考图为基础达到可用、清晰且视觉统一的桌面端效果。

## 19. 调研参考

### 017a 全站体验基础补充

- 用户端统一使用首页品牌 Header，主入口固定为：首页、比赛大厅、使用指南、我的页面；辩论进行页使用紧凑变体。
- Logo 删除“多人 · 多智能体 · 实时辩论”，改为具有东方书院意象和 AI 数字节点感的代码化 SVG 标识。
- 使用指南为单页，包含 4v4 流程、参赛步骤、比赛中操作和必要故障提示。
- 新增“我的页面”，包含资料、头像、当前比赛、比赛统计、最近比赛和设备检测状态。
- 真人和 Agent 使用不同的原创预设头像体系；真人在注册时选择头像，旧客户端未提交时由服务端随机持久化分配；Agent 头像唯一取自绑定 TTS 音色。用户可在注册后修改预设或上传头像；管理员只能在音色管理中调整 Agent 头像映射，不能在 Agent 管理中单独修改。
- 所有登录回跳必须保留完整站内 `pathname + search + hash`，`/lobby?join=1` 登录后仍自动打开输入房间号流程。
- 用户端和后台按钮统一使用有明确背景、文字、边框、hover、focus、disabled 和 loading 状态的语义颜色，不允许按钮与背景融为一体。

### 阿里云百炼

- [实时语音识别模型说明](https://help.aliyun.com/zh/model-studio/asr-model/)
- [实时语音识别使用指南](https://help.aliyun.com/zh/model-studio/real-time-speech-recognition-user-guide)
- [Fun-ASR 实时语音识别 WebSocket API](https://help.aliyun.com/zh/model-studio/fun-asr-realtime-websocket-api)
- [实时语音合成使用指南](https://help.aliyun.com/zh/model-studio/realtime-tts-user-guide)
- [Qwen-Audio-TTS 音色列表](https://help.aliyun.com/zh/model-studio/qwen-audio-tts-voice-list)
- [模型调用限流说明](https://help.aliyun.com/zh/model-studio/rate-limit)

### LiveKit 与后台参考

- [LiveKit Self-hosting](https://docs.livekit.io/home/self-hosting/)
- [LiveKit Egress](https://docs.livekit.io/home/egress/overview/)
- [satnaing/shadcn-admin](https://github.com/satnaing/shadcn-admin)
- [arhamkhnz/next-shadcn-admin-dashboard](https://github.com/arhamkhnz/next-shadcn-admin-dashboard)
- [marmelab/shadcn-admin-kit](https://github.com/marmelab/shadcn-admin-kit)

后台参考项目只用于提取表格、抽屉、行菜单、CRUD 和导航模式；本项目不复制模板代码、不引入 `ra-core`、Refine、Ant Design Pro 或 SQLAdmin 作为运行时框架。
