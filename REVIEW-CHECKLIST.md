# 任务完成检查表

Agent 不得仅凭“代码已写完”标记任务完成。每项需通过、标记不适用并说明原因，或作为明确阻塞报告。

## 范围与契约

- [ ] 改动只覆盖当前已批准规格，没有顺手增加功能或大范围重构。
- [ ] 行为符合 PRD，架构符合 TechDesign，语音结论没有超出 Spike 证据。
- [ ] 没有留下模板占位符、临时假数据或未说明的降级路径。
- [ ] HTTP、WebSocket、数据库和文件格式的契约已同步更新。
- [ ] 新依赖确有必要，符合既定技术栈，并记录选择理由。

## 代码质量

- [ ] Route/UI 未包含业务规则或直接数据库访问。
- [ ] 外部输入有运行时校验；内部类型精确，没有无理由的 TypeScript `any`。
- [ ] 异步任务可取消、可超时；队列和缓存均有明确上限。
- [ ] 没有吞异常、空 catch、无界重试或静默服务切换。
- [ ] 没有修改无关文件、重排无关格式或删除既有测试。

## 状态与并发

- [ ] 同一比赛的修改经过 MatchActor 串行化。
- [ ] 数据库提交后才更新 Actor 和广播事件。
- [ ] 命令幂等，事件 sequence 连续；跳号能够增量恢复或重取 snapshot。
- [ ] 异步回调校验当前 generation/attempt/connection/context，迟到结果不会污染状态。
- [ ] 房间角色区分组织者、辩手和观众；普通用户单未结束房间约束、全 Agent 管理员例外及一个房间一场比赛均有测试。
- [ ] 计时使用服务端 monotonic deadline，没有每秒写库或把浏览器倒计时当权威。
- [ ] 涉及名额、席位、观众或活动房间的竞态有数据库约束或事务测试。

## 实时语音与模型

- [ ] 非当前发言者音频在服务端被拒绝，不能只依赖客户端 mute。
- [ ] 人类发言中没有手动 mute/unmute；固定阶段和自由辩论获得发言权后都必须手动“开始发言”，等待超时不会由 Agent 接管。
- [ ] ASR 维持 16 kHz mono、100 ms frame、短 task 轮换和约 1 秒有界缓冲。
- [ ] TTS 使用选中 Agent 的唯一 task；Opus spool、PyAV 和 200-300 ms 总缓冲边界未被绕过。
- [ ] Agent 暂停、重置和 TTS 失败从头恢复，不实现精确 seek；正式 Agent 文字只来自实际播放内容。
- [ ] 自由辩论对当前方所有 Agent 并行快速决策并渐进显示“决策中/举手/跳过”；最终队列按人类优先、Agent 意愿排序；仅为选中 Agent 生成正式文本。
- [ ] LLM、ASR、TTS 的取消、一次重试、第二次失败暂停语义已覆盖。
- [ ] 用户看到渐进且不超前的字幕；正式 Agent 文字不被 TTS 改写。
- [ ] ASR 同时保存 raw final 与可编辑 display text；后续上下文读取 display text，LLM 输入快照锁定 context_version。
- [ ] 延迟、失败率、队列水位和关联 ID 可观测，日志不包含完整 prompt 或密钥。

## 安全与权限

- [ ] Diff 中没有 API Key、密码、token、`.env` 或 `api.md` 内容。
- [ ] 所有用户输入、文件路径、URL 参数和外部响应均在边界校验。
- [ ] 未登录、普通用户、房主、选手、观众和管理员权限分别验证。
- [ ] Session Cookie、密码哈希、撤销和单活动房间规则没有被客户端绕过。
- [ ] 管理操作、Key 查看和管理员比赛控制写入脱敏审计日志；重置按产品规则不记录独立重置事件。
- [ ] 管理员没有强制跳过发言/阶段权限；重置不产生独立业务事件。
- [ ] 新公开端点按 PRD 做登录与权限保护；MVP 未擅自增加验证码、短信、邮件或复杂限流产品流程。

## 用户体验

- [ ] 加载、空数据、断线、重试、暂停、容量不足和权限拒绝均有明确界面状态。
- [ ] 核心失败在右侧状态抽屉说明原因和下一步，Toast 不承载完整错误。
- [ ] 控件语义、禁用条件和二次确认符合权限规则。
- [ ] 桌面目标视口无溢出、遮挡和布局跳动；移动端专项适配不作为 MVP 完成条件。
- [ ] 动画只表达状态，不阻塞操作，并尊重 `prefers-reduced-motion`。
- [ ] 后台按钮、Badge、菜单和表格在正常/hover/focus/disabled/loading 状态均有明确对比度，不依赖父级继承文字色。
- [ ] 后台创建/编辑失败保留抽屉输入；危险操作有确认和影响说明；筛选/排序可从 URL 恢复。
- [ ] 用户、比赛和日志列表没有静默固定条数截断；分页总数和筛选一致，列表查询没有 N+1。

## 验证证据

- [ ] 相关单元、集成、契约和 E2E 测试通过。
- [ ] Lint、格式、类型检查和构建通过，无新增警告。
- [ ] Web 生产构建通过受控入口清理精确的 `apps/web/.next`，没有 CSS 优化告警；不得以增量构建产物直接发布 immutable 静态资源。
- [ ] UI 改动已用真实浏览器在目标桌面视口截图检查。
- [ ] 语音改动使用真实音频或记录帧验证，不能只用 mock 声称实时性能通过。
- [ ] 真实验收包含 50 路 LLM、180 秒真人 ASR、11 音色实际双 Opus 盲听和 Agent 响应间隔 P50/P95。
- [ ] 数据库 migration 在空库和已有数据升级路径上验证；未修改旧 migration。
- [ ] 未执行 Git 绕过、跳过 hook 或删除失败断言。

## 交接

- [ ] `MEMORY.md` 已更新当前状态、验证结果、风险和下一步。
- [ ] 当前规格的验收项已逐条对应证据，未完成项明确标注。
- [ ] 标准命令或工程结构发生变化时，已同步 `AGENTS.md` 和相关 `agent_docs/`。
- [ ] 最终说明包含改动摘要、验证命令与结果、未验证事项，不夸大完成度。
