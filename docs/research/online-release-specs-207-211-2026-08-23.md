# Specs 207–211 线上发布记录

日期：2026-08-23  
范围：v2.1 后台、赛制版本、运行诊断、实验批次与训练房间。

## 发布结果

- 生产数据库追加迁移至 `0036_experiment_batch_format`，未修改既有迁移或历史比赛。
- 发布顺序为数据库迁移、Core、Jobs、Web；LiveKit 保持运行。四项服务均 active，重启计数为 0。
- 生产备份包含发布前源码、Web standalone 和 PostgreSQL dump；同时保留 Web 回滚副本。
- 生产 preflight：健康/就绪检查 200，活动比赛数 0，实验目标版本 2.1.0，创建能力已开启。

## 线上验证

- 公网首页、实验入口和排行榜可访问；未授权管理接口返回 401。
- 未登录实验入口正确跳转登录页并保留回跳地址；公开页面无浏览器错误。
- 真实供应商探针通过：TTS、ASR、LLM、AI 裁判和 LiveKit 均完成一次短链路调用。
- 服务连续观察 5 分钟，10 次采样均无 warning、重启或活动比赛。
- 数据库只读审计确认 `experiment_batches`、`format_versions`、`prompt_templates`、
  `system_log_events` 及相关版本/请求索引存在，`alembic check` 无待生成操作。

## 尚未覆盖

本记录不替代管理员认证操作、真人麦克风与设备恢复、长时间多场比赛、并发压力或音色盲听验收。
这些项目应在正式招募前由研究团队现场完成并单独记录。
