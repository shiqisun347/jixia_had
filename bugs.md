当前仍存在太多太多 bug,需要全面的分析、各种测试、修复。应该全局考虑，不要只考虑某个功能的 bug, 忽略了整体的设计，要对整体的设计进行分析，考虑到各种可能的情况。

匿名用户访问公开页面时，`/api/auth/me` 的正常 `401 not_authenticated` 曾被误判为登录服务异常，导致抬头长期显示“重试登录状态”（154 已修复：认证 Query 将该精确响应归一化为匿名状态；真正的超时、网络错误和 `session_expired` 仍保留错误语义）。

创建房间只填写比赛名称、不选择题库辩题时，点击“创建并进入房间”原本没有任何错误提示，按钮像是失效（155 已修复：显示“请选择辩题”，聚焦并关联无障碍错误节点；未发送创建请求）。

大厅房间号输入区仍提示“不区分大小写”，与当前新房间统一生成 6 位纯数字房间号的用户规则不一致（156 已修复：改为“6 位数字房间号”，保留历史字母数字房间号读取兼容）。

当前头像修改不生效

头像更新后已经打开的房间/比赛页面仍可能显示旧头像（069 已修复：成功更新后失效 rooms/matches 查询并按 avatar_version 重载）。

赛后记录首次加载失败时，页面应保留明确的失败状态并允许用户重试，不应误显示为空记录（068 已修复并加入浏览器回归）。

赛后审阅中存在未保存文字时不应直接提交旧版本（100 已修复：提交按钮锁定并提示先保存或取消；取消/保存成功后恢复提交）。

当前大量按钮颜色与背景颜色过于接近，导致用户无法清楚地知道哪些是可点击的按钮，哪些是不可点击的按钮。需要对整体的 UI 设计进行优化，确保用户能够清楚地知道哪些是可点击的按钮，哪些是不可点击的按钮。全面进行分析，包括各个页面的按钮颜色、背景颜色、字体颜色等，确保整体的 UI 设计风格一致，用户体验良好，最好参考已有的成熟的、好看的UI 配色设计。

390px 窄视口的全局抬头原本将品牌、四个主导航和登录/注册挤在同一行，导致品牌截断且导航逐字竖排（114 已修复：窄视口第一行保留 Logo/账号，第二行四项导航平分水平显示）。

114 将窄视口抬头改为两行后，109 的页面 Toast 仍使用桌面固定偏移，390px 下会与导航底部轻微重叠（115 已修复：移动 Toast 使用两行抬头安全偏移并保留间距）。

114 修复两行抬头后，登录/注册认证 Shell 仍按单行 5.2rem 扣除，390px 登录页会多出约 18px 无意义滚动（116 已修复：抬头、Toast、AuthShell 共享 `--jx-header-height`）。

后台模态抽屉中的错误 Toast 曾覆盖标题、关闭按钮和表单，并且重复失败会堆叠相同提示（101 已修复：Toast 在抽屉正文内布局，同一活动消息去重）。

普通页面 Toast 原本固定在视口顶部 16px，注册页条款加载失败时会遮挡排行榜、使用指南、登录和注册导航（109 已修复：页面 Toast 统一位于全局抬头下方，模态 Toast 位置不变）。

普通页面 Toast 虽已移动到抬头下方，但卡片本体仍会拦截下方控件点击；资料页头像失败后无法再次点击“编辑资料”（117 已修复：Toast 非交互区域允许点击穿透，关闭按钮仍可操作）。

注册页和条款页的条款请求失败后原本只能整页刷新；Toast 消失后注册按钮仍永久禁用且没有页面内原因（110 已修复：两处均提供“重新加载条款”，保留已填表单并在成功后恢复提交）。

注册页条款失败增加持久页面内 `role=alert` 后，不应同时再发送内容重复的错误 Toast，否则读屏器会在同一次失败中播报两次（110b 已修复：保留唯一可操作的页面内错误）。

Toast 队列原本声称最多 3 条，但第 4 条不同消息到来时实际会显示 4 条（102 已修复：严格保留最新 3 条，淘汰消息可再次显示）。

排行榜加载中、无每日快照和搜索无结果曾统一显示“暂无匹配的排名”（103 已修复：明确区分加载、空快照、搜索无结果和请求失败）。

排行榜请求失败时原本只能整页刷新（104 已修复：页面内重新加载，重试期间显示加载态并防止重复请求）。

注册页头像单选项的无障碍名称曾暴露内部资源键 `human-01`～`human-16`（105 已修复：改为自然的“头像 1”～“头像 16”，保留原生单选键盘语义）。

个人资料二级抽屉仍沿用“选择头像 human-01”等内部名称，与注册页不一致（106 已修复：统一为自然编号并保留 `aria-pressed` 即时保存状态）。

登录/注册字段错误原本只有视觉红字，没有 `aria-invalid`、`aria-describedby` 或错误播报角色（107 已修复：字段与错误节点稳定关联并使用 `role=alert`）。

登录/注册密码字段原本把“显示密码”按钮嵌套在 label 内，浏览器会把字段读为“密码 显示密码”（111 已修复：label 仅通过 `htmlFor` 关联输入，显隐按钮和错误节点独立）。

正式站 Web standalone 构建原本没有注入 Core 端口，Next rewrite 回落到 8000；该端口返回 200 状态的 HTML 占位页，导致注册和所有 API 被误判为失败（112 已修复：构建从 `.env` 派生 `CORE_HOST`/`CORE_PORT`，并对产物 rewrite 做端口门禁）。

共享 API 客户端原本会将 HTTP 200 非 JSON 响应的原生 `SyntaxError` 直接传到页面，用户可能看到无法理解的解析错误（113 已修复：统一转为 `invalid_api_response` 及安全中文提示）。

资料编辑与修改密码弹窗原本没有与 107 一致的字段错误关联，且密码错误文本会混入字段的可访问名称（108 已修复：四个字段使用稳定 `aria-invalid`/`aria-describedby`/`role=alert`，错误节点移出 label）。

等待房间深色控制栏曾存在低透明度加入/设备按钮和禁用态（072 已修复：改为实色背景、边框、焦点和明确禁用态）。

辩论页面中间不再显示实时字幕；实时 ASR/Agent 文本只在文字记录内显示（082 已修复并在 108 复核实现）。

为什么辩论一直是正方一辩发言，但声音也发生了变化；文字记录框应该与左边的框对齐，且应该有滚动条，而不是一直扩充页面长度。

运行时 Agent 阶段曾出现当前发言席位元数据残留，导致页面显示旧席位（070 已修复：Agent 准备和播放开始均原子写入立场与席位）。

倒计时不可用（017c 已接入服务端权威剩余时间；076 修复刷新/重连；108 复核客户端 200ms 平滑倒计时与比赛浏览器回归通过）。

自由辩论阶段的倒计时 UI 重新设计，参考debate_page.png。应该在

不要这一行：字幕严格跟随服务端实际音频播放进度。

头像不生效；头像与音色绑定，自动在agent 管理中同步生效，不可在 agent 管理中修改头像；当前各个页面的头像显示不一致，有些正确显示，有些不正确显示（083 已修复：完整排行榜与首页使用统一头像目录，缺失资源保留首字降级；等待房间、比赛页、个人资料路径已保持版本化服务端头像）

个人页面中，编辑资料 和 修改头像，都应该是一个二级弹出窗口，而不是单独的页面。 选择你的默认头像 也应该是在这个二级弹出窗口中进行，而不是在最下面（084 已完成：编辑资料、头像、默认头像与普通修改密码均在资料抽屉/二级弹窗内；强制首次改密保留独立安全页面）

顶部栏修改：不要我的页面，现在应该是 首页 比赛大厅 排行榜 使用指南（048 已实现；108 复核全局抬头和账号导航专项通过）。

首页的排行榜只显示 agent 和 人类的 前三名，不应该是横排排列，而是类似于颁奖台，中间最高，左右两边依次降低。排行榜应该有一个二级弹出窗口，点击查看更多，进入排行榜页面，显示所有的排名。排行榜页面应该有一个搜索框，可以搜索用户的排名情况。排行榜页面应该有一个二级弹出窗口，点击查看更多，进入排行榜页面，显示所有的排名。排行榜页面应该有一个搜索框，可以搜索用户的排名情况。最好结合当前 ui，参考github已有的成熟的、好看的代码，确保整体的 UI 设计风格一致，用户体验良好。

顶部栏修改：点击用户信息，如果是普通用户，直接进入个人信息页面；如果是管理员，跟当前一样，有一个 我的页面和 管理页面的选择。 修改密码应该是在 我的页面中进行。 点击退出图标应该 有一个二级弹出窗口，确认是否退出，点击确认后才退出登录。（084 已完成普通改密二级弹窗；账号入口与退出确认此前已完成浏览器回归）

管理员作为参加比赛时，应该是普通用户身份，而不是管理员身份。比如在我的页面查看比赛历史时，应该是普通用户身份，而不是管理员身份。管理员身份应该只在管理页面中使用。（127 已修复比赛 HTTP/WS 指令边界：ADMIN 账号以普通辩手/观众身份进入比赛时不再自动获得 privileged；后台 `/api/admin` 能力保持不变。）

创建比赛时， 3 我的席位 是不需要的 在这里选择的，去除该部分， Agent 自动填满其他席位 为默认（077 已修复：创建页只保留比赛信息与辩题两个真实输入步骤，选席在等待房间完成，自动补位语义不变。）

选择席位环节，不是 “替换该 agent“，而是“选择该席位“；若该位置为真人，则提示申请交换席位，若该位置为 Agent，则提示选择该席位。申请交换席位流程已在 062 实现：确认弹窗、对方同意/拒绝、事务性交换和状态变化后的失效处理。

房间号应该为纯数字（049 已修复新建房间为 6 位数字；历史字母数字房间号仅保留读取兼容，不再生成）。

房间前台约每 1.5 秒同步服务端状态。 类似于这种信息，无需在页面显示，请全面检查（077 已复核：大厅移除“5 秒同步”，等待房间移除“Agent 席位恢复”；仅保留“已同步/同步中断”等用户需要的数据可信度状态。）

logo 应该使用 /Users/sunshiqi/code/JX_debate/logo.png ，应该大一些，这是我们平台的 logo，应该在所有页面中使用，确保整体的 UI 设计风格一致，用户体验良好。

主持人语音已在 064 增加阶段切换约 1 秒缓冲；075 已新增开场欢迎、各阶段感谢过渡和终场感谢，并以 10 段预生成音频发布为正式 4v4 v2。比赛过程中不临时调用 TTS；启用新版本时同 key 旧版本会在同一事务内停用。

主持音频结束推进不应依赖房主始终在线（071 已修复：在场真人辩手也可报告播放完成，观众不具备控制权限）。

主持音频结束命令服务端曾未校验路由传入的授权标记，观众可伪造 `host.finished` 推进比赛（073 已修复：MatchActor 在领域层拒绝未授权命令）。

等待房间“作为辩手加入”按钮的禁用态曾只降低透明度，在深色侧栏中不易识别（074 已修复：使用明确灰底、边框、文字和禁用光标）。

赛制规则页面已在 063 支持“编辑为新版本”和删除；有房间引用的规则不能破坏性删除，4v4 入口文案改为“新建 4v4 正式赛规则”。

主页刷新时，会出现 1s 左右，正在进行的三场比赛，刷新后又显示为空（065 已修复：短暂失败保留最近成功快照，失败时提供重新同步）

比赛时，头像上的小绿点应该在头像右下角，而不是在头像内（094 已修复圆形头像裁切；108 复核在线点使用头像容器绝对定位 `-bottom/-right` 并有独立在线语义）。

比赛连接状态放在底部控制栏，点击查看延迟、丢包率等信息（051 已实现；094 已重构底部分组；108 比赛专项回归通过）。

比赛时，提前结束发言需要统一确认弹窗（086 已实现；099 增加过期状态保护；108 修复其 React effect Lint 回归并通过比赛专项）。

比赛文字记录请求失败时不得显示为空记录，需明确提示并支持重试（067 已实现）。

对于所有弹窗确定，应该是跟 ui 一致的弹窗，而是系统弹窗（086 已完成：生产源码禁止原生 confirm/alert/prompt；恢复默认头像补齐统一确认，失败保留弹窗并显示原因）

比如我在当前mac上显示时,比赛时中间的一部分内容由于固定一屏,导致内容显示不全（017c 已重构为有界一屏布局；078 再次在 1280×720、1440×900、1920×1080 正式站验证无文档级滚动，竞技区和底部控制栏完整位于视口内。）

出现正方 1 辩和二辩都是同一个 agent 的情况,应该避免同一个 agent 在同一场比赛中占据多个席位.（061 已修复新房创建和开赛校验；078 只读核查确认 061 后正式站未再产生重复，当前暂停比赛的 8 个配置 Agent 均唯一。旧终态验收比赛保留当时快照，不篡改历史。）

首页最下端的排行榜参考 /Users/sunshiqi/code/JX_debate/排行榜.png 设计.

3 Agent 阵容与选席 应该直接没有,不需要显示（077 已移除并完成四视口回归。）

首页暂停中的比赛曾被标记为 LIVE（077 已修复：`PAUSED` 显示“已暂停”，`START_PENDING_RUNTIME` 显示“启动中”，只有 `RUNNING` 显示 LIVE。）

首页的排行榜 ❧ 排行榜 ❧ 每日根据正常完赛评分更新 不需要这一部分 ; 其次,排行榜部分似乎有点太大,看起来跟上面不协调, 需要调整大小，确保整体的 UI 设计风格一致,此外进一步美化细节（079 已修复并发布：删除重复总标题和说明，保留双榜前三名与完整指标；正式真实榜单模块从 612px 降至 435px。）

切换为观众, 试听录音,邀请加入,退出房间 等等 以及 房间内 各个按钮, ui/颜色/按下颜色 等等应该完全重新设计,当前仍存在不好看,颜色跟背景色过于接近需要对整体的 UI 设计进行优化,确保用户能够清楚地知道哪些是可点击的按钮,哪些是不可点击的按钮,全面进行分析,包括各个页面的按钮颜色、背景颜色、字体颜色等,确保整体的 UI 设计风格一致,用户体验良好,最好参考已有的成熟的、好看的UI 配色设计.（080 已修复并发布：等待房间主/次/危险/身份操作分级，正反方席位动作使用可读实色，统一焦点/按下/禁用态并通过自动对比度检查。）

辩论页面,顶栏和辩题栏之间有一个空栏,应该去除;（078 已移除空的 44px `debate-topbar`，辩题栏直接承接全局抬头，并完成正式站三视口验收。）

P0: 莫名其妙自动进入 实时服务发生异常，比赛已暂停，请检查设备后申请恢复; 我还正在房间,只要一刷新页面就自动显示比赛已暂停,且无法恢复比赛. 点击恢复显示,比赛指令未执行，请检查实时连接后重试。点击暂停,再点击恢复,显示同样,但是确实可以继续了.恢复以后,仍显示"实时服务发生异常，比赛已暂停，请检查设备后申请恢复。"（076 已修复：命令 ACK 携带提交后的权威快照，连续暂停/恢复不再使用旧 sequence；恢复倒计时清理旧错误；刷新重连采用 connection epoch，旧连接关闭不能覆盖新连接。真正的 Core 重启仍按既有规则进入人工恢复。）

比赛暂停或服务异常后，中央和席位区域仍显示当前发言者光圈、动态波形与“发言计时”，和实时链路已冻结的真实状态冲突（119 已修复：暂停态使用独立冻结卡，并取消活动席位、波形与发言计时视觉）。

人类的右下角一直是灰色,但我一直在网页里,也一直可以听到声音.（076 已修复：比赛 WebSocket 建连或重连后主动刷新房间成员快照，在线点跟随服务端连接租约。）

需要完整,全面梳理自动暂停逻辑,实时判断逻辑等等,应该尽可能清晰.借助 grill me skill 跟我对齐.（076 已完成相关根因修复：每名人类辩手使用独立 60 秒掉线计时，不再覆盖比赛主计时；当前发言者掉线时冻结并持久化实际剩余时间。发布审计也已覆盖全部非终态比赛，避免带活动比赛误判为空。）

应该有一个声音播放控制,用于多个辩手在同一房间时避免声音混乱或者暂时关闭声音
（081 已修复并发布：比赛底部增加“本机声音/恢复声音”二态控制，只修改当前设备的主持音频和 LiveKit 远端音轨 `muted` 属性；不暂停媒体、不关闭麦克风、不发送比赛命令、不影响其他用户。四视口 136/136 与正式站 1280×720 观战验证通过。）

比赛过程中的 文字记录 1. 应该每段文字都标注对应的阶段 2. 实时的 ASR 应该在最下面,而不是固定最上面 3. UI和排版优化,当前对用户查看历史文字记录不优化,容易搞混和不好找,应该 3. 底部 文字记录与全局上下文实时同步 不要这个小栏 4. 最终文字 不要 5 .全部正式发言 x条 不要 6. 实时同步 文字附近 (考虑美观性可以进行调整),放一个 一键复制 当前所有文字记录的功能, 以标准/清晰/阅读友好的格式（082 已修复并发布）

文字记录已有内部滚动，但此前只有短记录场景，长比赛记录的滚动边界缺少验证（121 已补充长记录 Storybook：抽屉内部滚动、页面不扩高、实时识别文字保持置底）。

比赛过程中,中间部分应该要显示当前所处的辩论阶段（085 已修复并发布：阶段来自冻结赛制和权威当前动作位置，运行状态作为次级说明）

重构当前的 UI 设计,当前的 UI设计不够美观,颜色跟背景色过于接近,需要对整体的 UI 设计进行优化,按钮颜色区分也不明显,也不好看. 需要重新调研 UI,最好直接使用已有的类似的,确保用户能够清楚地知道哪些是可点击的按钮,哪些是不可点击的按钮,全面进行分析,包括各个页面的按钮颜色、背景颜色、字体颜色等,确保整体的 UI 设计风格一致,用户体验良好,重新调研参考已有的成熟的、好看的UI配色设计. 尤其是按钮

重构当前比赛过程中底部的控制和信息栏,应该从用户的角度考虑,当前的设计杂乱,ui 也不清晰,排版也有问题,比如 发言按钮应该出现在控制栏中间,最左边是声音控制 最右边是 退出房间, 右 2 是 比赛控制 右 3 是 网络状态 大概这样（094 已重新设计：声音/发言/比赛三个稳定容器，中央发言为唯一主操作，网络/比赛控制/离开按层级固定在右侧；095 复核无权操作状态显示明确提示；四视口与 Storybook 已回归。）

不要这个栏,也不要在这个位置增加其他栏了 按阶段显示 · 实时内容置底（095 复核：不新增信息栏；紧凑视口隐藏重复阶段说明，保留中央阶段标题和实时内容。）

4/4 不要, x 方 和 立场 应该更大,更显著（093 已修复：侧栏不再显示 4/4；阵营名称与立场改为更大的固定两行状态槽，完整内容保留在 title 中。）

头像应该根据边框,是圆形,而不是正方形,各个地方都要修改（094 已修复比赛页真实裁切根因：图片相对圆形容器定位，席位和中央头像均保持圆形；其他主要页面此前已使用圆形容器。）

当前阶段应该更大的显示（094 已修复：当前阶段和中央状态标题提升一个视觉层级，同时保持 1280×720 一屏。）

设备警告态的下一步提示必须与真实动作一致（097 已修复：WARN 时顶部显示“确认网络提示并完成准备”，确认入口置于可选试听之前；PASS/FAIL 语义不变。）

房主点击开始比赛前应知道还缺什么（098 已修复：空席、未选席真人、离线、未准备和重复 Agent 会禁用开赛并显示具体原因；服务端仍做最终事务校验。）

比赛确认框打开后如果权威状态已经推进，不应继续允许旧动作（099 已修复：发言结束、暂停、Agent 状态变化或比赛终止会自动关闭过期确认框并提示未执行；有效确认和双击闸门不变。）

主持人发言时 中间部分应该为重新设计,而不是显示上一阶段人的头像或者其他信息（085 已使用独立主持卡；094 复核主持状态不渲染上一位辩手头像。）

不同的 agent 音量似乎差异很大,语速差距也很大. 比如明辩 的声音明显很小,其次,这个声音的音色也不太好听,有点抑郁 见微 的语速略微有点快
（122 已修复后台试听未应用 `playback_gain`、导致试听音量与正式比赛不一致的问题；试听现与比赛使用相同的固定增益和峰值保护。明辨音色观感、见微语速及 11 音色整体一致性仍需真人试听后校准，至少 10 人盲听尚未执行，不能写成已通过。）

后台列表的“操作”列只有浅色下箭头，用户不容易发现可点击的行菜单（120 已修复：共享后台操作触发器显示清晰的“操作”文字、白底边框和下箭头）。

应该可以显示 agent 的举手（091 已实现：Agent 决策候选按顺序显示“AI 第 N 位/将发言”，全部不愿发言时显示“AI 补位”；094 复核保留该状态。）

自由辩论时,每一方的进度条有 bug,应该是总时间的进度条,而不是每一轮重新变成 100%（094 已复核并强化展示：分母固定使用自由辩论阶段总时长，Storybook 以 138/300、162/300 验证为 46%/54%。）

自由辩论时,重置发言存在 bug,比如对方 agent 在发言,我申请了重置发言, 结果变成了我申请发言,轮到你发言了！我点了申请发言,也只是举手,然后比赛卡着了,对重置发言/申请发言等功能进行全流程的检查（087b 已修复：重置后重播不再清空对方真人举手队列，覆盖人类/Agent 重置回归；094 未改变命令语义。）

用户和 agent 头像怎么没了, 页面的 logo 怎么也没了（093 已修复并发布：standalone 发布包显式携带 public 资源，正式站 Logo、真人头像和 Agent 头像资源均返回 200。）

立场 字太小了,而且显示不全（093 已修复并发布：立场字号、字重与可用宽度提升，长文本最多两行完整换行，并保留完整 title。）

日志管理可以查看所有细节,比如模型的请求和返回结果,日志管理应该参考当前 github 上的成熟的/考虑服务器性能轻量的（126 已采用轻量分层：审计日志继续只展示脱敏管理操作；比赛管理按需打开“模型诊断”，最多读取该场最近 25 次调用，可查看脱敏输入、LLM 草稿、attempt、context version、Token、延迟、状态和错误码，不加载密钥、请求头或供应商原始响应。）

全面增强后台管理系统的增删改查功能

设备检测逻辑应该进一步简化,比如同一设备/xxx 避免重复的检测,或者其他可以简化的逻辑,这种简化同样适用于恢复比赛等

实时文字显示速度应该快于实时语音,否则会出现文字显示滞后于语音的情况,应该进一步优化,当前通常是文字显示滞后于语音
（124 已修复 Agent 字幕的确定性延迟源：字幕前缀按已送入 LiveKit 的音频进度额外领先 300 ms，抵消播放缓冲和业务 WebSocket 延迟；不改变正式文字、TTS 或音频。人类 ASR 代码链路无客户端节流，既有受控远程首 interim 约 228 ms，但真人公网麦克风听感仍需最终验收。）

管理员凭据不应写入缺陷文档或仓库；应由部署环境注入，登录后强制修改（已按安全要求移除明文凭据）。

agent的每次发言请求,应该包含 当前辩论的历史,以及必要的参数（125 已修复：请求使用最新 `display_text` 完整历史，并包含模型、阶段、下一阶段、持方、可读辩位、发言者和目标字数；不使用内部 action key 作为辩位。）

比赛结束页面,中间部分应该完全重新设计,应该提供AI judge 的评判结果展示（123 已补齐：赛后页自动轮询裁判处理中状态，完成后自动显示获胜方和分数；评分失败时授权房主/管理员可在页面重试；终止比赛不轮询、不评分）。

ASR 任务在超时、连接中断或异常重置后不得继续复用失效的 WebSocket，否则自动重试可能再次使用半失效连接（141 已修复：异常和取消统一取消发送协程、关闭并清空连接；正常连续任务仍复用连接）。

直接启动 Next standalone 时，HTML 不能在 CSS/JS chunk 404 的情况下看似返回成功（142 已修复：构建产物同时复制 `public/` 和 `.next/static/`，直接启动验证资源均为 200）。

Storybook 的等待房间失败状态必须与真实房间路由保持相同 Toast 安全区（143 已修复：故事显式提供 `/rooms/story` 测试 pathname，不改变正式路由判断）。

首页不应因为认证 barrel 导入而加载完整比赛运行与自由辩论代码（144 已修复：生产代码改用 auth 直接模块导入，首页初始资源减少约 32.5%，构建门禁阻止比赛代码再次进入首页）。

认证状态暂时不可用时，抬头的登录/注册入口仍必须保留 `/lobby?join=1` 等深链接参数（145 已修复：统一使用安全 return_to 生成入口，503 场景四视口回归通过）。

后台抽屉关闭后键盘焦点不应落到 body（146 已修复：共享 AdminDrawer 恢复创建按钮或对应操作菜单触发器，覆盖鼠标关闭和键盘 Escape）。

共享确认弹窗关闭后键盘焦点不应丢失（147 已修复：ConfirmDialog 恢复原按钮或后台操作菜单触发器，覆盖退出房间、比赛控制和管理确认等入口）。

窄桌面运行页打开文字记录后，键盘焦点和对话框语义不应丢失（148 已修复：抽屉使用 `role=dialog`/`aria-modal`，打开聚焦关闭按钮，Escape 关闭并恢复“文字记录”触发按钮；内容仍在抽屉内滚动）。

自由辩论 Agent 主动申请/兜底验收状态的中央阶段必须与状态一致（149 已修复：Storybook 夹具统一使用 `FREE_DEBATE` 当前动作，并加入阶段断言；不改变生产状态机）。

比赛运行页弹层在实时快照重渲染时不应抢走用户当前焦点，网络状态也必须支持标准键盘关闭（150 已修复：文字记录与网络状态统一使用 Radix Dialog，支持焦点陷阱、Escape/遮罩关闭、触发按钮恢复；父组件重渲染焦点稳定）。

开赛和恢复倒计时不能显示内部状态或提供重复恢复动作（151 已修复：`START_COUNTDOWN`/`START_PENDING_RUNTIME` 使用中文状态，`RESUME_COUNTDOWN` 显示“恢复倒计时”并隐藏重复申请按钮）。

自由辩论举手验收场景必须遵守当前 holder 方资格（152 已修复：真人与 Agent Storybook 夹具的 holder side 与席位一致，不改变领域规则）。

主持播报期间不得残留上一位辩手的活动席位、波形或计时（153 已修复：领域层进入 `HOST_ANNOUNCING` 原子清理发言身份与计时，Web 解析层忽略非发言 action）。

全面增强后台管理系统的增删改查功能

设备检测逻辑应该进一步简化,比如同一设备/xxx 避免重复的检测,或者其他可以简化的逻辑,这种简化同样适用于恢复比赛等

[$grill-me](/Users/sunshiqi/.codex/skills/grill-me/SKILL.md) 你应该参考 GitHub 成熟设计，代码等等， 后台应该支持比赛数据的批量管理,下载等,应该保留更加详细的比赛数据,比如agent 的每一次请求和回复等,举手队列等 全面增强后台管理系统的增删改查功能 全面增强系统的日志功能

自由辩论阶段,agent的不是有决策是否发言的逻辑吗? 那么应该在 agent 做完决策后及时举手,以方便团队互相了解状态,而不是不发言等着 agent 发言,在agent 开始发送请求时,就应该显示 在举手的位置显示 决策中 如果发言为举手,不发言显示 跳过, 但逻辑仍然是跟之前一样,如果所有人和 agent都不发言,那么最后仍是 agent 发言

（157 已完成本地实现：本方全部 Agent 独立渐进显示“决策中/举手/跳过”；人类始终优先，真人按服务端顺序、Agent 按意愿与席位排序；无人举手时按最高有效意愿或 `match_seed` 确定性补位。队内状态仅同方真人与管理员可见，快速决策使用完整最新辩论历史，结构化结果长期保存。待 PostgreSQL 与正式站真实链路验收。）

系统后台应该支持比赛数据的批量管理,下载等,应该保留更加详细的比赛数据,比如agent 的每一次请求和回复等,举手队列等

为什么发言顺序总是固定的？自由辩论每一方agent都是固定的顺序。每次都是同一个人发言，看看是哪里的问题 应该修复该问题

当前的比赛中断了，但是好像无法恢复
